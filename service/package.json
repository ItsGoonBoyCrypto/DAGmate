{
  "name": "dagmate-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DAGmate Kaspa L1 sidecar — non-custodial escrow/settlement builders and mainnet spikes, wrapping @kronsdk/kron-sdk. Own HD seed, own key layout, zero Dagger code or shared key material.",
  "scripts": {
    "start": "node server.js",
    "spike": "node spikes.mjs"
  },
  "dependencies": {
    "@kronsdk/kron-sdk": "0.17.1",
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
